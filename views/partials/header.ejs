<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
        <% getLocales().forEach( locale => { %>
        <a class="nav-link lang-link" href="/change-locale/<%= locale %>"><%= locale %></a>&nbsp;
        <% }) %><!-- / This will allow us and the user to manually change the language of the page -->
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/"><%= __('Home') %></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/ads"><%= __('Ads') %></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/help"><%= __('Help') %></a>
                </li>
            </ul>
        </div>
    </div>
</nav>